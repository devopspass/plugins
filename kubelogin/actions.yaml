install:
  object_types: application
  application_name: kubelogin
  title: Install kubelogin
  description: |
    Install kubelogin
  icon: fas fa-download
  playbook: plugins/kubelogin/playbooks/install.yml
  required_options:
    req_version:
      title: Version to install
      remember: true
      remember_key: name
      type: string
      default: latest
